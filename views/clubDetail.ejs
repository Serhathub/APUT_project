<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css" />
  <link rel="stylesheet" href="css/clubDetail.css">
  <title><%= club.name %> - Club Detail</title>
</head>
<body>
  <h1 class="text-center mb-4">Favoriete club: <%= club.tla %></h1>
  <%- include('partials/profileModal') %>
  <div class="container mt-5">
    <a href="/favorieten" class="btn btn-secondary mb-4">← Terug</a>

    <div class="club-detail-box">
      <div class="club-header mb-4">
        <div class="row align-items-center">
          <div class="col-md-3">
            <img src="<%= club.crest %>" alt="<%= club.name %>" class="img-fluid" />
          </div>
          <div class="col-md-6 text-center">
            <h2 class="fs-1"><%= club.name %></h2>
            <p><strong>Aantal keer LIVE gezien:</strong> <%= seen %></p>
            <form action="/api/favorites/seen" method="POST">
              <input type="hidden" name="clubId" value="<%= club.id %>">
              <button type="submit" class="btn btn-dark">Gezien</button>
            </form>
          </div>
        </div>
      </div>

      <div class="club-details mb-5">
        <table class="table table-bordered table-dark">
          <tbody>
            <tr><td><strong>Korte naam:</strong></td><td><%= club.shortName || '-' %></td></tr>
            <tr><td><strong>Manager naam:</strong></td><td><%= club.coach?.name || '-' %></td></tr>
            <tr><td><strong>Liga:</strong></td><td><%= club.league || '-' %></td></tr>
            <tr><td><strong>Club kleuren:</strong></td><td><%= club.clubColors || '-' %></td></tr>
            <tr><td><strong>Gevonden:</strong></td><td><%= club.founded || '-' %></td></tr>
            <tr><td><strong>Stadium:</strong></td><td><%= club.venue || '-' %></td></tr>
          </tbody>
        </table>
      </div>

<div class="mt-4">
  <h4>Eerste Elftal</h4>
  <table class="table table-striped table-dark">
    <thead>
      <tr>
        <th>Positie</th>
        <th>Naam</th>
      </tr>
    </thead>
    <tbody>
      <% lineup.forEach((player, index) => { %>
        <tr>
          <td>
            <% if (index === 0) { %>Keeper<% } %>
            <% if (index >= 1 && index <= 4) { %>Defender<% } %>
            <% if (index >= 5 && index <= 8) { %>Midfielder<% } %>
            <% if (index >= 9 && index <= 10) { %>Attacker<% } %>
          </td>
          <td><%= player.name %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
