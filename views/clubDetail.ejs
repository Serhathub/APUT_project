<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css" />
  <link rel="stylesheet" href="/css/clubDetail.css">
  <title><%= club.name %> - Club Detail</title>
</head>
<body>
  <h1 class="text-center mb-2">Favoriete club: <%= club.tla %></h1>
  <%- include('partials/profileModal') %>
  <div class="container mt-5">
    <a href="/favorieten" class="btn btn-secondary"><span class="fs-4">← Terug</span></a>

    <div class="club-detail-box">
      <div class="club-header mb-4">
        <div class="row align-items-center">
          <div class="col-md-5 ">
            <h2 class="fs-1"><%= club.name %></h2>
            <p><strong>Aantal keer LIVE gezien:</strong> <span id="count-<%= club.id %>" class="fs-4"><%= seen %></span></p>
            <button class="btn btn-secondary" onclick="incrementCount('<%= club.id %>')">Gezien</button>
          </div>
          <div class="col-md-3">
            <img src="<%= club.crest %>" alt="<%= club.name %>" class="img-fluid" />
          </div>
        </div>
      </div>

      <div class="club-details mb-5">
        <table class="table table-bordered table-dark">
          <tbody>
            <tr><td><strong>Korte naam:</strong></td><td><%= club.shortName || '-' %></td></tr>
            <tr><td><strong>Land:</strong></td><td><%= club.area.name || '-' %></td></tr>
            <tr><td><strong>Manager naam:</strong></td><td><%= club.coach?.name || '-' %></td></tr>
            <tr><td><strong>Liga:</strong></td><td><%= leagueName || '-' %></td></tr>
            <tr><td><strong>Club kleuren:</strong></td><td><%= club.clubColors || '-' %></td></tr>
            <tr><td><strong>Gevonden:</strong></td><td><%= club.founded || '-' %></td></tr>
            <tr><td><strong>Stadium:</strong></td><td><%= club.venue || '-' %></td></tr>
            <tr><td><strong>Website:</strong></td><td><%= club.website || '-' %></td></tr>
          </tbody>
        </table>
      </div>

<table class="table table-striped table-dark">
        <th class="text-center fs-2">Eerste Elftal</th>
</table>
<div class="text-center mt-4">
  <div class="row justify-content-center mb-4">
    <div class="col-auto">
      <div class="card p-5 bg-dark text-white">
        <div><strong><u><span class="fs-5">Keeper</span></u></strong></div>
        <div><span class="fs-5"><%= lineup[0].name %></span></div>
      </div>
    </div>
  </div>
  <div class="row justify-content-center mb-4">
    <% for (let i = 1; i <= 4; i++) { %>
      <div class="col-auto">
        <div class="card p-5 bg-dark text-white">
          <div><strong><u><span class="fs-5">Defender</span></u></strong></div>
          <div><span class="fs-5"><%= lineup[i]?.name %></span></div>
        </div>
      </div>
    <% } %>
  </div>
  <div class="row justify-content-center mb-4">
    <% for (let i = 5; i <= 8; i++) { %>
      <div class="col-auto">
        <div class="card p-5 bg-dark text-white">
          <div><strong><u><span class="fs-5">Midfielder</u></span></strong></div>
          <div><span class="fs-5"><%= lineup[i]?.name %></span></div>
        </div>
      </div>
    <% } %>
  </div>
  <div class="row justify-content-center">
    <% for (let i = 9; i <= 10; i++) { %>
      <div class="col-auto mx-4">
        <div class="card p-5 bg-dark text-white">
          <div><strong><u><span class="fs-5">Attacker</span></u></strong></div>
          <div><span class="fs-5"><%= lineup[i]?.name %></span></div>
        </div>
      </div>
    <% } %>
  </div>
</div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/incrementCount.js"></script>
</body>
</html>
